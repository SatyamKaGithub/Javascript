// De-structuring arrays and objects

// Copying an item, which then becomes independent of the original.

let {PI} = Math;
console.log(PI)


let {pi} = Math;            // lowercase pi is not available in the Math obect 
console.log(pi)

