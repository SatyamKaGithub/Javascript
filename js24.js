// LET keyword

// We cannot access a let variable before we declare it
// We can declare an unassigned variable with let
// We can't redeclare a let variable
// We can reassign a variable

let user;
console.log(user) // here it will give undefined output
user = "satyam"
console.log(user)